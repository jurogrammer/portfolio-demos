# 규칙 기반 질문-응답-답장 자동화 플랫폼 MVP 상세 기획서

---

## 1. 프로젝트 개요

### 1-1. 서비스 명칭 (가칭)

**ReframeBot** — 매일 하나의 질문으로 사고 패턴을 리프레이밍하는 교육/학습 플랫폼

### 1-2. 서비스 요약

기수제(30명/기수)로 운영되는 익명 웹 서비스. 시스템이 매일 1회 학습 질문을 발송하고, 사용자가 응답하면 관리자가 사전 설정한 규칙(키워드, 카테고리, 언어 패턴)에 따라 자동 답장을 생성하여 웹 내 메시지함으로 전달한다. 축적된 질문-응답-답장 데이터셋은 향후 AI 자동화 고도화의 학습 데이터로 활용한다.

### 1-3. 핵심 목표

| 구분 | 목표 |
|---|---|
| MVP 검증 | 규칙 기반 답장 자동화 모델의 기술적 구현 가능성 확인 |
| 데이터 축적 | 구조화된 질문-응답-답장 데이터셋 확보 (AI 학습용) |
| 사용자 가치 | 매일 1회 질문을 통한 성찰/학습 습관 형성 |
| 운영 효율 | 관리자 개입 최소화, 반자동 답장 시스템 구축 |

---

## 2. 사용자 흐름 (User Flow)

### 2-1. 참여자 흐름

```
[가입] 닉네임+이메일 입력 → 기수 배정 (관리자 수동)
  ↓
[매일] 오전 9시, 오늘의 질문이 메시지함에 도착
  ↓
[응답] 질문에 대해 텍스트 응답 작성 (1일 1회 제한)
  ↓
[답장 수신] 규칙 매칭 → 자동 답장 생성 → 메시지함에 도착
  ↓
[히스토리] 지난 질문-응답-답장 타임라인 열람
```

### 2-2. 관리자 흐름

```
[질문 관리] 일일 질문 콘텐츠 등록/예약
  ↓
[규칙 관리] 키워드·카테고리별 답장 템플릿 등록
  ↓
[모니터링] 응답 현황 대시보드 확인
  ↓
[검수] 규칙 미매칭 응답 → 수동 답장 작성/규칙 보완
```

---

## 3. 기능 정의서

### 3-1. 사용자 기능

| ID | 기능명 | 설명 | 우선순위 |
|---|---|---|---|
| U-01 | 회원가입 | 닉네임 + 이메일. 익명 운영 (실명 미수집) | P0 |
| U-02 | 로그인/로그아웃 | 이메일 기반 매직링크 로그인 (비밀번호 없음) | P0 |
| U-03 | 오늘의 질문 확인 | 메시지함에서 당일 질문 확인 | P0 |
| U-04 | 응답 제출 | 질문에 대한 텍스트 응답 (1일 1회, 수정 불가) | P0 |
| U-05 | 답장 확인 | 시스템/관리자가 보낸 답장을 메시지함에서 확인 | P0 |
| U-06 | 히스토리 | 과거 질문-응답-답장 타임라인 열람 | P1 |
| U-07 | 프로필 수정 | 닉네임 변경 | P2 |
| U-08 | 알림 배지 | 새 질문/답장 도착 시 미읽음 표시 | P1 |

### 3-2. 관리자 기능

| ID | 기능명 | 설명 | 우선순위 |
|---|---|---|---|
| A-01 | 관리자 로그인 | 이메일+비밀번호 인증 | P0 |
| A-02 | 기수 관리 | 기수 생성, 참여자 배정/이동, 기수 상태(모집중/진행중/종료) | P0 |
| A-03 | 질문 등록 | 질문 텍스트 + 카테고리 + 발송일 지정. 예약 발송 지원 | P0 |
| A-04 | 질문 목록 관리 | 등록된 질문 조회/수정/삭제, 발송 이력 확인 | P0 |
| A-05 | 규칙 관리 | 답장 생성 규칙 CRUD (키워드, 카테고리, 템플릿) | P0 |
| A-06 | 응답 모니터링 | 기수별/질문별 응답 현황, 응답률 통계 | P0 |
| A-07 | 수동 답장 처리 | 규칙 미매칭 응답 목록 확인 → 수동 답장 작성 | P0 |
| A-08 | 대시보드 | 전체 현황 (기수별 참여율, 응답률, 자동/수동 답장 비율) | P1 |
| A-09 | 데이터 내보내기 | 질문-응답-답장 데이터셋 CSV/JSON 다운로드 | P1 |

### 3-3. 시스템 기능

| ID | 기능명 | 설명 | 우선순위 |
|---|---|---|---|
| S-01 | 질문 자동 발송 | 매일 지정 시각, 해당 기수 참여자에게 질문 발송 (크론잡) | P0 |
| S-02 | 규칙 매칭 엔진 | 응답 텍스트 분석 → 키워드/카테고리 매칭 → 템플릿 선택 | P0 |
| S-03 | 답장 자동 생성 | 매칭된 템플릿에 변수 치환 후 답장 생성 | P0 |
| S-04 | 미매칭 플래깅 | 규칙에 매칭되지 않는 응답 → 관리자 검수 큐에 추가 | P0 |
| S-05 | 데이터 파이프라인 | 모든 질문-응답-답장을 구조화된 데이터셋으로 자동 저장 | P0 |

---

## 4. 규칙 엔진 설계

MVP의 핵심 기능인 규칙 기반 답장 생성 시스템의 구조.

### 4-1. 규칙 매칭 흐름

```
[사용자 응답 제출]
  ↓
① 키워드 매칭 — 응답 텍스트에서 사전 등록된 키워드 탐색
  ↓
② 카테고리 매칭 — 질문 카테고리 + 응답 감정/태도 분류
  ↓
③ 템플릿 선택 — 매칭된 규칙의 답장 템플릿 선택
  ↓
④ 변수 치환 — {닉네임}, {키워드}, {원문일부} 등 동적 삽입
  ↓
⑤ 발송 — 메시지함에 답장 등록
  ↓
(미매칭 시) → 관리자 검수 큐
```

### 4-2. 규칙 데이터 모델

```
Rule {
  id: UUID
  name: string              // 규칙 명칭 (예: "부정적 자기인식 리프레이밍")
  priority: number          // 우선순위 (낮을수록 먼저 매칭)
  question_category: string // 적용 대상 질문 카테고리
  condition_type: enum      // KEYWORD | PATTERN | SENTIMENT | CATEGORY
  condition_value: string   // 매칭 조건값 (예: "못하|안되|실패|포기")
  template_id: FK           // 연결된 답장 템플릿
  is_active: boolean
  created_at: timestamp
}

ReplyTemplate {
  id: UUID
  name: string              // 템플릿 명칭
  body: text                // 답장 본문 (변수 포함)
                            // 예: "{닉네임}님, '{키워드}'라는 표현이 눈에 띄네요.
                            //      혹시 이렇게 바꿔 생각해보면 어떨까요? ..."
  variables: string[]       // 사용 가능 변수 목록
  category: string          // 리프레이밍 / 격려 / 심화질문 / 정보제공
  created_at: timestamp
}
```

### 4-3. 매칭 조건 타입

| 타입 | 설명 | 예시 |
|---|---|---|
| KEYWORD | 특정 키워드 포함 여부 (OR 조건, 정규식 지원) | `못하\|안되\|실패` |
| PATTERN | 문장 패턴 매칭 (정규식) | `나는.*할 수 없` |
| SENTIMENT | 긍정/부정/중립 감정 분류 (단순 사전 기반) | `NEGATIVE` |
| CATEGORY | 응답 내용 카테고리 분류 | `자기비하`, `회피`, `성찰` |

MVP에서는 KEYWORD와 PATTERN을 우선 구현하고, SENTIMENT/CATEGORY는 사전 기반 간이 분류로 시작한 후 AI 고도화 시 모델 기반으로 전환한다.

---

## 5. 데이터 모델 (ERD)

### 5-1. 주요 테이블

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   cohorts    │     │    users     │     │  questions   │
├──────────────┤     ├──────────────┤     ├──────────────┤
│ id (PK)      │────<│ id (PK)      │     │ id (PK)      │
│ name         │     │ nickname     │     │ body         │
│ capacity (30)│     │ email        │     │ category     │
│ status       │     │ cohort_id FK │     │ scheduled_at │
│ start_date   │     │ role         │     │ cohort_id FK │
│ end_date     │     │ created_at   │     │ is_sent      │
│ created_at   │     └──────────────┘     │ created_at   │
└──────────────┘                          └──────────────┘
                                                │
                    ┌───────────────────────────┘
                    ▼
┌──────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  responses   │     │    replies       │     │     rules        │
├──────────────┤     ├──────────────────┤     ├──────────────────┤
│ id (PK)      │────>│ id (PK)          │     │ id (PK)          │
│ user_id FK   │     │ response_id FK   │     │ name             │
│ question_id  │     │ body             │     │ priority         │
│ body         │     │ rule_id FK (null)│     │ question_category│
│ submitted_at │     │ is_auto          │     │ condition_type   │
│ is_read      │     │ created_by       │     │ condition_value  │
└──────────────┘     │ created_at       │     │ template_id FK   │
                     └──────────────────┘     │ is_active        │
                                              └──────────────────┘

┌──────────────────┐     ┌──────────────────┐
│ reply_templates  │     │   messages       │
├──────────────────┤     ├──────────────────┤
│ id (PK)          │     │ id (PK)          │
│ name             │     │ user_id FK       │
│ body             │     │ type (질문/답장)  │
│ variables[]      │     │ ref_id           │
│ category         │     │ is_read          │
│ created_at       │     │ created_at       │
└──────────────────┘     └──────────────────┘
```

### 5-2. 데이터셋 저장 구조 (AI 학습용)

모든 상호작용은 `datasets` 테이블에 비정규화하여 별도 저장. 향후 AI 파인튜닝 데이터로 직접 활용 가능한 형태.

```
datasets {
  id: UUID
  cohort_id: FK
  question_body: text
  question_category: string
  response_body: text
  response_submitted_at: timestamp
  reply_body: text
  reply_is_auto: boolean
  matched_rule_id: FK (nullable)
  matched_keywords: string[]
  admin_edited: boolean       // 관리자가 수동 수정했는지
  quality_score: number       // 향후 관리자 평가용
  created_at: timestamp
}
```

---

## 6. 화면 설계

### 6-1. 사용자 화면

| 화면 | 주요 구성 | 비고 |
|---|---|---|
| 랜딩 페이지 | 서비스 소개, 현재 모집 기수 안내, 참여 신청 CTA | 비로그인 상태 |
| 로그인 | 이메일 입력 → 매직링크 발송 안내 | 비밀번호 없음 |
| 메시지함 (홈) | 오늘의 질문 카드 + 미읽음 답장 목록 | 로그인 후 기본 화면 |
| 질문 상세 | 질문 본문 + 응답 작성 폼 + (제출 후) 내 응답 표시 | 1일 1회 제출 |
| 답장 상세 | 답장 본문 + 원래 질문/응답 컨텍스트 표시 | 읽음 처리 |
| 히스토리 | 날짜별 타임라인 (질문→응답→답장 세트) | 스크롤 페이지네이션 |
| 프로필 | 닉네임 수정, 기수 정보, 참여 통계 | 간단한 설정 |

### 6-2. 관리자 화면

| 화면 | 주요 구성 |
|---|---|
| 대시보드 | 기수별 참여율, 오늘 응답률, 미처리 답장 건수, 자동/수동 비율 차트 |
| 기수 관리 | 기수 목록 + 생성/편집. 참여자 목록 + 배정 |
| 질문 관리 | 질문 목록 (캘린더뷰), 등록/수정/삭제, 발송 상태 |
| 규칙 관리 | 규칙 목록 + 생성/편집. 조건 설정 UI + 템플릿 연결 |
| 템플릿 관리 | 답장 템플릿 CRUD. 변수 미리보기 |
| 검수 큐 | 미매칭 응답 목록, 원문 확인 + 수동 답장 작성 + 규칙 추가 제안 |
| 데이터 내보내기 | 기간/기수 필터 → CSV/JSON 다운로드 |

---

## 7. API 명세 (주요 엔드포인트)

### 7-1. 사용자 API

```
POST   /api/auth/magic-link       이메일로 매직링크 발송
POST   /api/auth/verify            매직링크 토큰 검증 → 세션 생성
DELETE /api/auth/session            로그아웃

GET    /api/messages                메시지함 목록 (질문 + 답장)
GET    /api/messages/:id            메시지 상세 (읽음 처리)

GET    /api/questions/today         오늘의 질문
POST   /api/questions/:id/respond   응답 제출
GET    /api/history                 히스토리 (페이지네이션)

PATCH  /api/profile                 닉네임 수정
```

### 7-2. 관리자 API

```
POST   /api/admin/auth/login        관리자 로그인
POST   /api/admin/auth/logout       관리자 로그아웃

GET    /api/admin/dashboard/stats   대시보드 통계

CRUD   /api/admin/cohorts           기수 관리
POST   /api/admin/cohorts/:id/users 참여자 배정

CRUD   /api/admin/questions         질문 관리
POST   /api/admin/questions/:id/send  질문 즉시 발송

CRUD   /api/admin/rules             규칙 관리
CRUD   /api/admin/templates         템플릿 관리

GET    /api/admin/responses         응답 목록 (필터: 기수, 날짜, 매칭상태)
GET    /api/admin/review-queue      검수 큐 (미매칭 응답)
POST   /api/admin/responses/:id/reply  수동 답장

GET    /api/admin/datasets/export   데이터 내보내기
```

### 7-3. 내부 API (시스템)

```
POST   /api/internal/cron/send-questions   질문 발송 크론 (매일 09:00)
POST   /api/internal/match-and-reply       응답 → 규칙 매칭 → 답장 생성
```

---

## 8. 기술 스택 제안

### 8-1. 프론트엔드

| 항목 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | Next.js 15 (App Router) | SSR/SSG 지원, API Routes 통합 |
| 언어 | TypeScript | 타입 안정성, 규칙 엔진 타입 정의 |
| 스타일링 | Tailwind CSS | 반응형 빠른 구현 |
| UI 컴포넌트 | shadcn/ui | 커스터마이즈 용이, 접근성 기본 지원 |
| 상태관리 | Zustand | 가벼운 클라이언트 상태 (메시지 읽음 등) |
| 폼 처리 | React Hook Form + Zod | 유효성 검증 |

### 8-2. 백엔드

| 항목 | 선택 | 근거 |
|---|---|---|
| 런타임 | Next.js API Routes + Server Actions | 프론트엔드와 통합, 별도 서버 불필요 |
| 인증 | NextAuth.js (Auth.js) | 매직링크 + 관리자 Credentials 이중 프로바이더 |
| DB | Supabase (PostgreSQL) | 호스팅된 PostgreSQL, Row Level Security, 실시간 기능 |
| ORM | Prisma | 타입 안전 쿼리, 마이그레이션 관리 |
| 크론잡 | Vercel Cron / Supabase pg_cron | 매일 질문 발송 스케줄러 |
| 검색/매칭 | 자체 구현 (TypeScript) | MVP 수준 키워드/정규식 매칭 엔진 |

### 8-3. 인프라

| 항목 | 선택 | 근거 |
|---|---|---|
| 호스팅 | Vercel | Next.js 최적화 배포, 자동 스케일링 |
| DB 호스팅 | Supabase | 무료 티어로 MVP 충분, PostgreSQL 관리형 |
| 파일 저장 | Supabase Storage | 향후 이미지 첨부 등 확장 대비 |
| 모니터링 | Vercel Analytics + Sentry | 성능/에러 추적 |

### 8-4. 향후 AI 고도화 스택 (참고)

| 항목 | 선택 | 시점 |
|---|---|---|
| 감정 분석 | OpenAI API / KoELECTRA | Phase 2 |
| 답장 생성 | GPT-4o Fine-tuning | Phase 3 (데이터 충분 축적 후) |
| 벡터 DB | Supabase pgvector | Phase 2 (유사 응답 검색) |

---

## 9. 시스템 아키텍처

```
┌─────────────────────────────────────────────────┐
│                    Vercel                        │
│  ┌──────────────┐  ┌────────────────────────┐   │
│  │  Next.js App │  │   API Routes           │   │
│  │  (사용자 웹)  │  │   /api/admin/*         │   │
│  │  (관리자 웹)  │  │   /api/auth/*          │   │
│  └──────┬───────┘  │   /api/internal/*       │   │
│         │          └────────────┬─────────────┘  │
│         │                      │                 │
│  ┌──────┴──────────────────────┴──────────┐     │
│  │         Server Actions / Middleware      │     │
│  │    ┌─────────────────────────────┐      │     │
│  │    │     Rule Matching Engine    │      │     │
│  │    │  (키워드/패턴/감정 매칭)     │      │     │
│  │    └─────────────────────────────┘      │     │
│  └─────────────────────┬──────────────────┘     │
│                        │                         │
│  ┌─────────────────────┴──────────────────┐     │
│  │           Vercel Cron Jobs              │     │
│  │    - 매일 09:00 질문 발송               │     │
│  │    - 매 시간 미매칭 리마인더             │     │
│  └─────────────────────────────────────────┘     │
└───────────────────────┬─────────────────────────┘
                        │
              ┌─────────┴─────────┐
              │     Supabase      │
              │  ┌─────────────┐  │
              │  │ PostgreSQL  │  │
              │  │  (메인 DB)  │  │
              │  ├─────────────┤  │
              │  │   Auth      │  │
              │  ├─────────────┤  │
              │  │  Storage    │  │
              │  └─────────────┘  │
              └───────────────────┘
```

---

## 10. 정책 정의

### 10-1. 사용자 정책

| 정책 | 내용 |
|---|---|
| 익명성 | 닉네임만 노출. 이메일은 로그인 용도로만 사용, 타 사용자에게 비공개 |
| 응답 제한 | 1일 1회, 제출 후 수정 불가. 자정(00:00) 기준 리셋 |
| 기수 정원 | 기수당 최대 30명 |
| 응답 기한 | 질문 발송일 23:59까지. 미응답 시 해당 질문 만료 |
| 콘텐츠 제한 | 욕설/비하/개인정보 포함 응답은 시스템 필터링 후 관리자 검수 |

### 10-2. 관리자 정책

| 정책 | 내용 |
|---|---|
| 검수 SLA | 미매칭 응답은 24시간 이내 수동 답장 처리 |
| 질문 예약 | 최소 3일 전 질문 등록 권장. 당일 등록 시 즉시 발송 가능 |
| 규칙 버전 관리 | 규칙 수정 시 기존 버전 보존 (소프트 업데이트) |

### 10-3. 데이터 정책

| 정책 | 내용 |
|---|---|
| 보존 기간 | 서비스 종료 후 1년간 보존 후 익명화 처리 |
| AI 학습 동의 | 가입 시 데이터 활용 동의 수집 (옵트인) |
| 내보내기 형식 | CSV (분석용), JSON (AI 학습용) 두 가지 지원 |

---

## 11. 개발 일정 (8주 기준)

| 주차 | 단계 | 주요 작업 |
|---|---|---|
| 1주 | 설계 | DB 스키마 확정, API 상세 설계, 와이어프레임 확정 |
| 2주 | 기반 구축 | 프로젝트 셋업, 인증 시스템, DB 마이그레이션 |
| 3주 | 사용자 핵심 | 메시지함, 질문 확인, 응답 제출, 답장 확인 |
| 4주 | 규칙 엔진 | 규칙 매칭 엔진 구현, 자동 답장 생성, 검수 큐 |
| 5주 | 관리자 | 기수/질문/규칙/템플릿 관리 CRUD, 대시보드 |
| 6주 | 자동화 | 크론잡 질문 발송, 데이터 파이프라인, 내보내기 |
| 7주 | UI/UX 완성 | 반응형 디자인 완성, 인터랙션, 로딩/에러 상태 |
| 8주 | QA/배포 | 통합 테스트, 버그 수정, Vercel 배포, 운영 가이드 |

---

## 12. 향후 고도화 방향

### Phase 2 — 감정 분석 자동화 (데이터 500건+ 축적 후)

Supabase pgvector를 활용하여 유사 응답 검색을 도입하고, 감정 분류를 사전 기반에서 경량 NLP 모델(KoELECTRA)로 전환하여 매칭 정확도를 높인다.

### Phase 3 — AI 답장 생성 (데이터 2,000건+ 축적 후)

축적된 질문-응답-답장 데이터셋으로 GPT-4o를 파인튜닝하여 답장을 AI가 직접 생성하도록 전환. 관리자는 검수/승인 역할로 전환된다.

### Phase 4 — 완전 자동화

AI 답장 품질이 안정화되면 자동 발송으로 전환하고, 관리자는 예외 케이스만 처리. 결제 시스템 도입으로 유료 서비스 전환.

---

## 13. 산출물 목록

| 산출물 | 형식 | 포함 내용 |
|---|---|---|
| 상세 기획서 | 본 문서 | 기능정의, 화면설계, 정책, 규칙엔진 설계 |
| UI/UX 디자인 | Figma | 와이어프레임 + 디자인 시안 (사용자/관리자) |
| DB 스키마 | Prisma Schema + ERD | 테이블 정의, 관계, 인덱스 |
| API 문서 | Swagger/OpenAPI | 전체 엔드포인트 명세 |
| 소스 코드 | GitHub | 프론트엔드 + 백엔드 + 규칙 엔진 |
| 배포 환경 | Vercel + Supabase | 운영 환경 설정 완료 |
| 운영 가이드 | Markdown | 관리자 사용법, 규칙 등록 가이드 |
